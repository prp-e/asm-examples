STK SEGMENT
	DW 100 DUP(?)
STK ENDS

DTS SEGMENT
	STR DB 80 DUP(?)
	LEN DB ?
	MAX DB 80
DTS ENDS

CDS SEGMENT
	ASSUME CS:CDS, DS:DTS, SS:STK
	MAIN PROC FAR
		MOV AX, SEG DTS
		MOV DS, AX
		MOV DX, OFFSET MAX
		MOV AH, 0AH
		INT 21H
		MOV AH, 4CH
		INT 21H
	MAIN ENDP
CDS ENDS
END MAIN
